Du fortsætter arbejdet på FLOWA-hjemmesiden (repo azam-stack/flowa-website, branch main).

KONTEKST
- Den godkendte UI/UX-plan ligger her: https://claude.ai/artifact/XwUWx2R6jmShiLT91Ab1E1
  Læs den først (action: read) og følg den til punkt og prikke.
- Fase 1, 2 og 3 er allerede eksekveret, committet og pushet til main.
  Seneste commit: 65e2fe5 "Phase 3 — Core sections rebuilt on the shared system".
  Se git log for fase 1 (d1f58a3) og fase 2 (518a927).
- Fase 4, 5 og 6 mangler.

OPGAVE 1: RESEARCH-PAS (blev blokeret i forrige session)
Forrige session kunne ikke nå 21st.dev, help.21st.dev og dribbble.com
(403 connect_rejected fra proxyen). Netværkspolitikken er nu opdateret.
- Gå ind på https://21st.dev/ og https://dribbble.com/ og find referencer
  til: hero med produktvisualisering, sammenligningstabel, prisafsnit
  "pris pr. møde", FAQ + kontaktformular kombineret, team-sektion for
  to stiftere, client-logo marquee.
- Skeln tydeligt mellem (a) research du faktisk har lavet nu, (b) eksisterende
  viden og (c) dine egne anbefalinger. Hvis en side stadig er blokeret,
  skriv den præcise URL og fejlen. Påstå aldrig at have set en blokeret side.
- Opsummer fund i en kort liste og foreslå KUN ændringer, der passer inden
  for den eksisterende plan. Ingen nye sektioner, ingen omskrivning af planen.

OPGAVE 2: EKSEKVER FASE 4, 5 OG 6
- Fase 4: Interaktion og motion (hero-entrance, reveal 480 ms, steps-linje,
  tabel-pill stagger, nav-underline, formular-feedback). Meget er allerede
  delvist på plads. Verificer og færdiggør.
- Fase 5: Responsivt og edge cases. Mobilhøjde skal ned på max 7500 px
  (er 10687 px nu; pricing, FAQ/get-started, team og offer er for høje på mobil).
- Fase 6: Kontrast (inkl. de orange labels), tastatur/skip-link,
  OG-billede 1200x630, JSON-LD FAQ synkroniseret til 4 spørgsmål,
  README og RESTRUCTURE_REPORT opdateret.

REGLER
- Commit og push efter hver fase med en klar commit-besked.
- Verificer hver fase med en Playwright screenshot-sweep på 1440 og 390 px
  (per-sektion klip, ingen horisontal overflow, ingen console-fejl,
  ingen [placeholder]-tekst). Chromium ligger i /opt/pw-browsers.
- npm run build skal være grøn (den kører scripts/check-content.mjs først).
- Al tekst ligger i src/content/site.en.ts. Manglende input fra stifterne
  udelades, aldrig vist som placeholder.
- Rør IKKE ved: sprog/valuta (beslutning B), e-mailadressen,
  formular-endpoint, priser, bios og LinkedIn-URLs. De afventer mig.
- Spørg ikke undervejs. Brug planens egne anbefalinger ved åbne valg
  og lav en kort liste over de valg, du traf, til sidst.
